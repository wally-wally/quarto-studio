# Postgres (Phase 1+)
DATABASE_URL=postgres://quarto:quarto@localhost:5432/quarto_studio
# 렌더 타임아웃(ms)
QUARTO_RENDER_TIMEOUT_MS=60000
# 렌더된 HTML 아티팩트 저장 디렉토리(웹·워커 공유). 기본 ./data/artifacts
ARTIFACT_DIR=./data/artifacts
